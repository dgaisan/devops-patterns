# Docker container serving static content using Apache HTTP Server

## build an image
`docker build -t my-static-content .`

## Run container
`docker run -p 3004:80 my-static-content`

## Run container in detached mode
`docker run -d --name my-apache-container -p 3004:80 my-static-content`